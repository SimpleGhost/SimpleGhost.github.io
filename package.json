{
  "name": "food",
  "version": "1.0.0",
  "description": "test",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "fakeDB": "json-server --watch db.json"
  },
  "author": "serega",
  "license": "ISC",
  "devDependencies": {
    "json-server": "^0.16.3",
    "webpack": "^5.4.0",
    "webpack-cli": "^4.2.0"
  }
}
